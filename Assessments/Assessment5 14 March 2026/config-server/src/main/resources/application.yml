server:
  port: 6001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Sumit-1018/microservices-config-repo.git
          default-label: main
eureka:
  client:
    service-url:
      defaultZone: http://localhost:6000/eureka/